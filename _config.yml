# Jekyll config for GitHub Pages
# This disables Jekyll processing so our HTML files are served as-is
# (Important: without this, files starting with _ may be ignored)

plugins:
  - jekyll-relative-links

relative_links:
  enabled: true
  collections: true

include:
  - frontend
  - backend
